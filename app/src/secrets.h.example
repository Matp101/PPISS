/*
 * WiFi credential list — copy this file to secrets.h and add your
 * networks.  The station will try each entry in order until one connects.
 *
 * wifi_cred_t is defined in wifi.c before this file is included.
 */

#define HAS_WIFI_CREDS

static const wifi_cred_t WIFI_CREDS[] = {
    { "YourSSID", "YourPassword" },
    /* { "AnotherSSID", "AnotherPass" }, */
};

#define WIFI_CRED_COUNT (sizeof(WIFI_CREDS) / sizeof(WIFI_CREDS[0]))
